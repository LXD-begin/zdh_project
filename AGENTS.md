# 项目规则

## 适用范围

- 本规则适用于 `E:\AI测试学习项目` 整个项目。
- 未经说明，不扩展到其他项目或其他工作区。

## 技术与环境约束

- 项目语言为 Python。
- Python 版本固定为 3.12。
- 技术栈以自动化测试代码为主，主要包括：
  - `pytest`
  - `requests`
  - `playwright`
  - `selenium`
- 如需使用其他第三方库，必须先确认该库与当前自动化测试场景直接相关，不能擅自增加无关依赖。

## 代码生成规则

- 每次生成代码时，必须为关键步骤、业务逻辑、公共方法和不易理解的代码补充对应的中文注释。
- 参数名、函数名、类名等标识符保持英文。
- 除代码标识符外，注释说明使用中文。
- 生成的代码必须符合初级自动化测试人员的理解水平，结构清晰、命名直观、易于团队维护。
- 避免生成过度封装、过度抽象、晦涩难懂或具有明显 AI 表达风格的代码。
- 优先使用简单、直接、常见的自动化测试写法；除非业务确实需要，不使用复杂设计模式、动态代码或多层封装。
- 代码设计要符合自动化测试框架的常规分层与职责划分。
- 不允许擅自添加用户未要求的功能、字段、流程、依赖或模块。
- 生成 pytest 测试用例时，如果使用 `pytest.mark.parametrize` 做数据驱动，必须提供 `ids` 参数说明。
- `ids` 必须使用中文，说明内容要同时关联业务场景和关键测试数据，确保用例失败时能从 pytest 报错信息中快速判断失败原因。
- `ids` 不允许使用无业务含义的编号、英文缩写、默认参数值、`success/fail` 等模糊描述。
- `ids` 的数量和顺序必须与参数化测试数据完全一致。
- 生成 UI 自动化测试用例时，所有执行失败的用例都必须按照失败步骤截图；截图说明或文件名要能体现用例名称、失败步骤和执行时间，方便定位问题。
- 生成接口自动化测试用例时，所有通过 `requests` 发起的接口请求，必须把完整请求信息记录到 `outputs/logs` 日志中，包括 method、url、headers、params、json/data/body、files、timeout 等实际请求内容；密码、token、cookie 等敏感信息需要脱敏后记录。
- 每条测试用例涉及到的测试数据都必须记录到 `outputs/logs` 日志中，包括参数化数据、Excel 行数据、动态生成数据、前置接口返回并被当前用例使用的关键数据；日志内容必须能关联到具体用例和业务场景。

## 代码修改规则

- 修改代码时，遵循最小改动原则。
- 只能修改当前需求直接相关的代码。
- 不允许顺带修改无关模块、无关逻辑、无关命名或无关结构。
- 修改前必须先明确指出需要修改的文件和位置。
- 修改说明必须包含“修改前”和“修改后”的对比内容。
- 未经说明，不直接大范围重构原有代码。
- 修改任何代码前，必须先提供拟修改文件、具体位置、修改前后对比及影响范围。
- 影响范围至少包括：涉及的文件、调用关系、测试用例、公共方法、配置或执行结果。
- 完成修改方案说明后，必须等待用户二次确认；用户明确同意后才能实际修改代码。
- 用户未确认时，只允许进行只读检查、问题分析和方案设计，不得写入或改动项目文件。
- 实际修改必须严格控制在用户确认的范围内；如修改过程中发现需要扩大范围，必须重新说明影响并再次确认。

## 目录结构约定

- `common` 目录存放公共方法，例如：连接数据库、打印日志、文件操作、二次封装的 `requests` 请求、读取 Excel 测试用例文件。
- `tools` 目录存放封装的工具类方法，例如：全局变量存储、提取请求结果作为全局变量、请求参数占位符替换全局变量、数据库断言、响应断言、常用造数方法。
- 代码级测试用例统一存放在 `testcase` 目录中。
- 所有涉及 `pytest` 的前置和后置处理统一放在 `testcase/conftest.py` 中，不得分散定义在测试用例文件或其他业务文件中。
- 前置和后置处理必须根据当前测试场景的真实业务需求实现，不得添加与当前业务无关的数据准备、清理、登录、数据库操作或其他逻辑。
- `fixture` 之间的嵌套依赖最多为两层，不允许超过两层。
- 能通过单个 `fixture` 完成的业务准备不得拆分成多层嵌套；没有真实前后置需求时，不得为了套用框架而强行生成 `fixture`。
- `data` 目录存放 Excel 测试用例文件。
- `outputs` 目录存放执行产物，其中 `reports` 目录存放生成的 Allure 报告，`logs` 目录存放日志文件，日志文件按年月日划分目录。
- 根目录新增 `run.py` 作为主执行入口，支持一键收集所有用例并执行。

## 输出要求

- 说明修改方案时，优先给出明确的修改点。
- 展示代码改动时，必须体现修改前后的对应关系。
- 若需求本身不完整，应先指出缺失信息，不擅自补全业务规则。
